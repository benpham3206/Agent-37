import test from 'node:test'; import assert from 'node:assert/strict'; import fs from 'node:fs'; import { DEFAULT, policyFeatures } from './policy.js';
test('node feature order matches shared policy contract',()=>{const contract=JSON.parse(fs.readFileSync(new URL('../contracts/policy.json',import.meta.url),'utf8'));assert.deepEqual(DEFAULT,contract.features);const values=policyFeatures({dimension:'minecraft:the_nether',terrain:{support_center:1,support_forward:1,support_back:1,support_left:1,support_right:1,drop_forward:0,drop_back:0,drop_left:0,drop_right:0,clearance_forward:1,clearance_back:1,clearance_left:1,clearance_right:1,hazard_near:0},target:{},self:{}},'zombie');assert.equal(values.length,contract.features.length);assert.equal(values[28],1);});
